"""Protocol-level concerns built on the JSON-RPC foundation.

Currently provides :mod:`mcp.protocol.errors` — the §22 error-handling model and the
authoritative error-code registry. Later phases add discovery (§7–§8), capabilities
(§9), and the feature methods.
"""
