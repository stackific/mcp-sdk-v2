"""Feature lifecycle and deprecation governance (§27).

Provides the three-state model (:mod:`mcp.lifecycle.state`), the deprecation policy and
transition rules (:mod:`mcp.lifecycle.policy`), the consolidated registry of deprecated
features, and the runtime deprecation-warning emitter (:mod:`mcp.lifecycle.registry`).
"""
