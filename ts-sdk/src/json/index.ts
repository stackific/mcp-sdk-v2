/**
 * JSON value model, numeric handling, and forward-compatibility utilities — S02.
 *
 * Covers §2.3 (JSONValue / object / array semantics), §2.5 (numeric handling),
 * and §2.6.2 (`_meta` key naming).
 */

export * from './value.js';
export * from './meta-key.js';
